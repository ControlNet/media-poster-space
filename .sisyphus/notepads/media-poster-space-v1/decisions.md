- Plan lines 6/125 anchor the bootstrap: monorepo roots under `apps/web`, `apps/desktop`, `packages/core` and packages scoped as `@mps/web`, `@mps/desktop`, `@mps/core` (per the filter names used throughout, e.g. lines 180-297).
- Root script style must center on `pnpm -w` + Turbo: `pnpm -w install --frozen-lockfile`, `pnpm -w turbo run lint typecheck test build`, `pnpm -w turbo run e2e:web e2e:desktop`, `pnpm -w turbo run build:release`, and the verify scripts such as `pnpm -w verify:release-artifacts` / `pnpm -w verify:docs-parity` (plan lines 48-813 show these commands as mandatory QA/gate references).
- Evidence/QA artifacts belong under `.sisyphus/evidence` (line 82), and CI should touch `.github/workflows/*` + `turbo.json`, `package.json`, `playwright.config.*`, `vitest.config.*` (line 355).
- Repository is also required to ship a minimal README and AGPL license at the root (line 125), to satisfy the open-source governance called out by AGENTS.md + plan.
- 2026-02-23: Chose placeholder echo scripts across packages to keep turbo tasks resolvable until real implementations are added in later tasks.
- 2026-02-23: Decided to keep actual turbo binary invocation for root `turbo` script so bootstrap commands align with plan checks without altering downstream packages.
- 2026-02-23: For Task 2, standardized shared core contracts around provider-neutral IDs (`id`, `providerId`, `libraryId`) and generic metadata/artwork keys to avoid exposing Jellyfin schema details in UI-facing models.
- 2026-02-23: Chose a strict validator strategy (whitelisted keys + type checks + date/number constraints) so Desktop/Web can hard-reject contract drift or provider-specific leakage at runtime.
- 2026-02-23: Exported a single public entrypoint (`@mps/core`) plus subpath exports (`types`, `provider`, `validation`) so both app packages can import contracts from stable boundaries.
- 2026-02-23: Accepted the current monorepo scaffold and placeholder scripts as Task 1 compliant, avoiding additional changes since verification commands already pass against the existing layout.
- 2026-02-23: For Task 2 follow-up, kept source contracts unchanged (already compliant) and applied test-only corrections to minimize churn while increasing boundary guarantees (`VisualItem` leakage checks + public export smoke coverage).
- 2026-02-23: Standardized focused boundary verification on direct vitest execution under the `@mps/core` filter to avoid root turbo script argument interpretation.
- 2026-02-23: Kept all Jellyfin-specific network/auth/media mapping logic behind `JellyfinMediaProvider` and exported it through `src/providers`, leaving shared provider-neutral contracts untouched.
- 2026-02-23: Standardized Task 3 error mapping to deterministic categories only (`network`, `auth`, `cors`, `version`, `unknown`) for Jellyfin provider failures/preflight diagnostics.
- 2026-02-23: Enforced no-secret-leak policy by using static sanitized error messages and adding explicit failure tests that assert credentials/tokens are not echoed in thrown provider errors.
- 2026-02-23: Task 4 design foundation is centralized in `packages/core/src/tokens` and re-exported from `@mps/core` + `@mps/core/tokens` to keep Desktop/Web token consumption consistent.
- 2026-02-23: Tailwind theme extension in `apps/web/tailwind.config.ts` is token-driven via CSS variables (`--mps-*`) rather than hard-coded feature-level values, preserving cinematic dark theming with dynamic accent injection.
- 2026-02-23: Brand typography baseline uses `Soehne Breit`/`Soehne Buch` with automatic fallback chains (`Avenir Next`/`Segoe UI`/`system-ui`) and explicit fallback mode toggled by `data-brand-font="fallback"`.
- 2026-02-24: For Task 7 desktop-only remember-password, chose a minimal encrypted local vault abstraction inside `apps/desktop` (WebCrypto AES-GCM + PBKDF2 over device-id + per-entry salt) instead of introducing Tauri/native secret storage, keeping scope inside Task 7 and avoiding Task 13 platform extras.
- 2026-02-24: Enforced logout behavior to clear session and password artifacts immediately (before remote invalidate completion) so the <=1s acceptance timing is deterministic while preserving `mps.onboarding.remembered-server` and `mps.onboarding.remembered-username`.
- 2026-02-24: Task 8 ingestion boundary stays provider-agnostic by routing through `MediaProvider.listMedia` and post-filtering results to selected `libraryId` values + non-empty `poster.url`; provider-specific shaping remains in adapters (e.g., Jellyfin).
- 2026-02-24: Manual refresh is exposed as `MediaIngestionRuntime.refreshNow()` in core and consumed by app wall UIs through `manual-refresh-button`, while periodic refresh cadence is fixed to `MEDIA_INGESTION_REFRESH_INTERVAL_MS = 300000`.
- 2026-02-24: For Task 8 QA parity, implemented a lightweight in-wall diagnostics disclosure (`diagnostics-open`) instead of a shared diagnostics framework, to avoid overlap/conflict with future Task 15 while still exposing deterministic acceptance evidence.
- 2026-02-24: Task 9 scheduler keeps provider-agnostic purity by using only `MediaItem` contract fields (`id`, `title`/`sortTitle`/`originalTitle`, `tags`, `people`, `year`) for suppression logic, with no provider adapter hooks or taste-based weighting.
- 2026-02-24: Exposed scheduler-facing alias constants (`SCHEDULER_*`) in addition to internal poster defaults so future task integrations can depend on stable naming without introducing a cache subsystem in Task 9.
- 2026-02-24: For Task 10, kept OLED safety logic provider-agnostic in `@mps/core/oled` and consumed it from web scene frame hooks, avoiding network/onboarding coupling while making cadence/risk behavior unit-testable.
- 2026-02-24: Preserved `/wall?mode=test` compatibility by expanding route parsing into a resolver that accepts either explicit test mode or cinematic seed/profile query mode, then reusing that resolver for both route gating and wall runtime config.
- 2026-02-24: For Task 11, kept interaction state inside existing onboarding wall runtimes (web + desktop) rather than introducing shared cross-package UI abstractions, to keep scope constrained and avoid overlap with later tasks.
- 2026-02-24: Chose fixed `DETAIL_CARD_TRANSITION_MS = 280` and wall-local keyboard listener lifecycle so transition bounds (240–320ms) and ESC close behavior remain explicit and testable across both surfaces.
- 2026-02-24: For Task 11 gap closure, kept detail metadata fallback removal strict (omit overview/meta nodes when unavailable) instead of rendering placeholder copy, so missing fields are hidden rather than narrated.
- 2026-02-24: Aligned controls-container transition duration with detail-card constant (`280ms`) to keep Task 11 transition surfaces in the same tested 240–320ms band.

- 2026-02-25: Catalogued TTL+capacity caches (node-lru-cache, quick-lru), LRU hotness flows, reconnect/backoff strategies (socketcluster-client, Apollo RetryLink), and offline-first hydration/persistence (TanStack Query hydrate/persist) to guide Task 12 insertion points.
- 2026-02-25: Implemented Task 12 cache policy as a new provider-agnostic core module (`@mps/core/cache`) and kept runtime persistence wiring inside app onboarding runtimes to avoid widening ingestion/scheduler APIs beyond the task scope.
- 2026-02-25: Recovery behavior keeps stale cached posters visible during `refreshing/error` transitions and only clears cache on explicit empty `ready` responses, prioritizing offline-first startup continuity.
- 2026-02-25: Desktop-specific preheat exception is modeled as cache-entry `preheated` scoring bias (not limit bypass), preserving mandatory hard caps while improving startup poster retention under eviction pressure.
- 2026-02-25: Reconnect strategy uses app-layer exponential backoff timers (2s base, 60s cap) driven by ingestion error state, with auth-triggered reconnect guidance surfaced immediately via `[data-testid="reconnect-guide"]`.
- 2026-02-25: Task 13 pattern catalog covers Lexical `SELECTION_CHANGE_COMMAND` hooks, `tauri-plugin-autostart-api` toggles, keyring-backed secret plumbing, Linux fallback paths, and Tauri packaging configs to guide the new desktop runtime wiring.
- 2026-02-25: Flagged the insecure fallback in Epic Asset Manager (storing tokens in `gio::Settings`) so Task 13 favors guarded keyring/secret-service layers or explicit user-visible fallbacks.
- 2026-02-25: Task 13 uses a typed `DesktopPlatformBridge` abstraction as the only onboarding integration boundary, with Rust commands implementing real desktop behavior and localStorage-backed defaults only for non-Tauri test/runtime fallback.
- 2026-02-25: Portable mode now hard-disables persisted autostart (UI + bridge init) while still allowing display selection persistence, matching desktop exception scope without expanding into Task 14+ platform behavior.
- 2026-02-25: Task 14 web behavior keeps password persistence impossible (`canPersistPassword: false`) and scopes restore to server+username only; no web storage password keys or backend runtime dependencies were introduced.
- 2026-02-25: Chose Vite `base: "./"` plus `vite build` in `@mps/web` build script to keep output static-deployable with relative asset paths while preserving existing onboarding/wall runtime routing.
- 2026-02-25: Task 15 crash export format is JSON-only package content (`logs + version + configSummary`) with strict recursive redaction of token/password/raw auth header keys and bearer/header string patterns before serialization.
- 2026-02-25: Retention policy is enforced in diagnostics log stores with hard defaults (`7d`, `100MB`) and surfaced verbatim in diagnostics UI for deterministic QA assertions.
- 2026-02-25: Task 18 docs parity uses a small repo-native verifier script (`scripts/verify-docs-parity.mjs`) instead of adding new dependencies, with capability checks driven by code markers in runtime/bridge files plus required-doc text assertions.
- 2026-02-25: Strict mode policy is fail-closed for unsupported claims. Unknown capability rows may exist, but if they claim support (`Yes`), verification fails until that claim is backed by implementation and mapped in verifier truth.
- 2026-02-25: For Task 16 fixed-seed causality, standardized a cross-platform `visualBaseline.seedProof` contract in metrics (`renderInput`, fixed-seed hashes, variant-seed hash) so threshold verification can prove seed consumption instead of trusting metadata-only seed fields.
- 2026-02-25: Kept platform-specific render-input expectations in verifier (`/wall?seed=...&profile=balanced` for web, `desktop-seeded-evidence-frame?seed=...` for desktop) to enforce deterministic and auditable seed injection paths without broadening scope into non-gate runtime features.
- 2026-02-25: Task 17 artifact verification is fail-closed on desktop realism: portable artifacts must be structurally valid tar payloads with expected `desktop-portable/` internals and manifest semantics, while Linux AppImage must be executable and checksum-linked to the linux portable artifact.
- 2026-02-25: Stable-channel release docs verification remains explicit by requiring deterministic metadata fields and exact artifact entry ordering, plus root/release changelog content parity and mandatory changelog headings.
- 2026-02-25: For Task 18 final audit, kept a no-change outcome because the current docs/parity corpus is already fail-closed and implementation-backed; adding edits without a concrete mismatch would increase drift risk.
- 2026-03-01: Task 9 evidence artifacts now recorded under `.sisyphus/evidence/task-9-desktop-parity-tests.txt` and `.sisyphus/evidence/task-9-desktop-reconnect-logout.txt` to document the required log capture for parity gates and reconnect/logout flow.
