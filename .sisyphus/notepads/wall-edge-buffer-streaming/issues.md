# Issues

- 2026-03-13 T2 executor blockage: session `ses_31abf93edffep2HrsxNFC6Js9p` returned stale T1 diffs across three retries without adding the required backlog/result-contract symbols or tests. Use fresh execution context for T2 and verify concrete symbols before trusting completion.
- 2026-03-15 T6 verification wrinkle: `pnpm --filter @mps/web e2e -- e2e/onboarding-auth.spec.ts --grep "..."` forwarded a literal `--` into Playwright in this repo and executed the broader suite instead of the intended grep subset. Direct `pnpm exec playwright test --config=playwright.config.ts --project=web-chromium ...` targeting was required, and the combined clock/fullscreen grep only ran stably with `--workers=1` because the default 2-worker run intermittently timed out before `onboarding-finish`.
- 2026-03-15 T6 verification wrinkle: Parallel/hand-started Vite servers conflicted with Playwright's managed `webServer` and produced misleading failures (`Port 4173 is already in use`, disabled manual refresh during overlap). Kill any stray `vite --host 127.0.0.1 --port 4173 --strictPort` process before the clean acceptance rerun.
- 2026-03-15 T10 blocker verification note: the exact full workers=1 web onboarding Playwright file command remained intermittently unstable from the managed Vite webServer side (  after several passing tests), even after the idle-hide fix landed; isolated blocker coverage passed, so treat any later full-file refusal as a separate infra/flakiness issue rather than part of the diagnostics-close contract.
- 2026-03-15 T10 blocker verification note: the exact full workers=1 web onboarding Playwright file command remained intermittently unstable from the managed Vite webServer side (page.goto ERR_CONNECTION_REFUSED after several passing tests), even after the idle-hide fix landed; isolated blocker coverage passed, so treat any later full-file refusal as a separate infra/flakiness issue rather than part of the diagnostics-close contract.
- 2026-03-15 T10 flake fix note: the healthy-stream/remount web spec could observe mediaRequestCount increment before the UI finished clearing manual-refresh disabled state, so the next click path needed a DOM-state poll instead of relying on the earlier count assertion alone.
- 2026-03-15 T10 onboarding-finish flake note: this spec previously clicked onboarding-finish immediately after login-submit, so heavy suite load could beat the library-selection render; guarding on library-checkbox-movies-main readiness made the click deterministic in both targeted and workspace e2e runs.
- 2026-03-15 T10 remembered-login flake note: a bare 5s post-login visibility check was insufficient for the restart spec under heavier runs; matching the hardened library-checkbox readiness pattern (visible + checked, explicit timeout) made the subsequent sessionStorage clear/reload deterministic.
- 2026-03-15 T10 remembered-login refinement: under load, clicking preflight and immediately filling login fields could race the async preflight rerender, leaving the first-step form in a half-stable state; waiting for the preflight response before filling credentials eliminated the retry-only login/library timeout.
