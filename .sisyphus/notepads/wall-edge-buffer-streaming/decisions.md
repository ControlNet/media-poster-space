# Decisions

- 2026-03-13 T1: Exported `WallPosterRowDirection`, `WallPosterRect`, `WallPosterEntryBufferEligibility`, and pure collect/resolve helpers from `presentation-sections.ts` so later shared-core applier work can reuse a DOM-free geometry contract across web and desktop.
- 2026-03-13 T1: Kept `collectEntrySideWallPosterWritableIndices` aligned with the existing edge traversal semantics, then derived `collectOrderedWallPosterWritableIndices` separately so the plan's "two nearest fully off-screen entry tiles" rule is enforced without changing legacy direction ordering behavior.
- 2026-03-13 T2: Exported stream state/result types plus `applyWallPosterGridStreamState` so later tasks can exercise the backlog contract directly in shared core without requiring real DOM nodes; tile state stays structural (`tile` object + `posterThumb.style`) for deterministic tests.
- 2026-03-13 T2: Kept `WALL_POSTER_GRID_STREAM_APPLIER_KEY` wired to a boolean compatibility wrapper that only maps `unavailable` to `false`, because web/desktop runtimes still branch on truthiness and are explicitly out of scope for this task.
- 2026-03-13 T2 follow-up: Lock the element-bound applier contract in shared-core tests through `createWallPosterGridSection(..., { items: [] })` plus a mock `ElementFactory`, so the keyed property is verified to return `WallPosterGridStreamApplyResult` without needing jsdom or host runtime changes.
- 2026-03-13 T2 follow-up: The earlier temporary boolean wrapper is superseded; the shared-core element applier now returns the raw `WallPosterGridStreamApplyResult` object end-to-end, and host truthiness handling remains an out-of-scope caller concern.
- 2026-03-13 T2: Final shared-core contract exports `WallPosterGridStreamApplier` and returns the full `WallPosterGridStreamApplyResult` through `WALL_POSTER_GRID_STREAM_APPLIER_KEY`; host runtimes remain out of scope and will adopt the richer contract in later tasks.
- 2026-03-13 T3: Kept `WallPosterGridRowState` structurally compatible by deriving row direction from the existing `entryIndices` ordering instead of widening the exported row-state shape; T3 uses `resolveWallPosterEntryBufferEligibility(...)` on a fresh per-row geometry snapshot rather than reusing row-wide traversal as writable eligibility.
- 2026-03-13 T3: `applyWallPosterGridStreamState` now builds per-pass writable snapshots (`orderedWritableIndices` + consumed writable orders) for each row, so backlog items drain FIFO before newly discovered posters while each row remains capped to its current two-slot entry buffer and never rewrites viewport-intersecting tiles in healthy flow.
- 2026-03-15 T8: The web regression spec tracks real mounted wall buttons via per-element `data-task8-probe-id` attributes assigned in-page during the test, then compares poster-thumb `backgroundImage` snapshots instead of selector numbering so visible-poster identity is asserted against actual viewport-intersecting elements.
